# Claude through the Link (POST /v1/messages). The Link runs without them, reporting "claude": false.
anthropic>=1.8                # --claude api: the Messages API with ANTHROPIC_API_KEY
claude-agent-sdk>=0.2.159     # --claude claude-code: your Claude subscription through Claude Code
# Optional: advertise _catalyst-link._tcp over mDNS so the tablet finds the Link by itself.
zeroconf>=0.130
# Optional, Windows: the media remote for the tablet's home mode (what the PC plays, play/pause/next, volume).
winrt-Windows.Media.Control>=2.0; sys_platform == "win32"
winrt-Windows.Storage.Streams>=2.0; sys_platform == "win32"
winrt-Windows.Foundation>=2.0; sys_platform == "win32"
pycaw>=20240210; sys_platform == "win32"   # a volume level; without it the volume keys still work
pillow>=10                                   # album art the tablet can show

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core events only. Everything else goes through this app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
